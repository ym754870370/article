/**
 * 定义接口访问错误时的处理方法
 */
// import Toast from '../../ui/base/src/toast/toast_plugin';

// export const commonApiSolution = (data) => {
//     const msg = data && data.msg;
//     if (msg) {
//         Toast(msg);
//     }
// };

// export default commonApiSolution;
